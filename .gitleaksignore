# Supabase ANON keys are designed to be public in client code (RLS-protected).
# These are default fallback values, never used in production.
lib/supabase.ts:jwt:4
lib/backend/supabase/client.ts:jwt:8

# README.md curl example — fake/illustrative Bearer token, not a real secret
backend/README.md:curl-auth-header:448
