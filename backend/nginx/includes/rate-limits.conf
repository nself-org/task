# Generated by nself build - DO NOT EDIT MANUALLY
# Rate limiting zones
# Configurable via RATE_LIMIT_API_RPS, RATE_LIMIT_AUTH_RPS, RATE_LIMIT_AI_RPS

limit_req_zone $binary_remote_addr zone=general:10m rate=2r/s;
limit_req_zone $binary_remote_addr zone=graphql_api:10m rate=100r/m;
limit_req_zone $binary_remote_addr zone=auth:10m rate=30r/m;
limit_req_zone $binary_remote_addr zone=uploads:10m rate=5r/m;
limit_req_zone $http_authorization zone=user_api:10m rate=1000r/m;
limit_req_zone $binary_remote_addr zone=static:10m rate=1000r/m;
limit_req_zone $binary_remote_addr zone=webhooks:10m rate=30r/s;
limit_req_zone $binary_remote_addr zone=functions:10m rate=50r/m;

# Sprint 33: configurable rate zones
limit_req_zone $binary_remote_addr zone=api:10m rate=30r/s;
limit_req_zone $binary_remote_addr zone=auth_strict:10m rate=5r/s;
limit_req_zone $binary_remote_addr zone=ai:10m rate=10r/s;
limit_req_zone $binary_remote_addr zone=webhook:10m rate=100r/s;

# Per-tenant rate limiting (keyed on X-Tenant-ID header for JWT-authenticated paths)
limit_req_zone $http_x_tenant_id zone=tenant_api:10m rate=30r/s;
limit_req_zone $http_x_tenant_id zone=tenant_ai:10m rate=10r/s;

limit_conn_zone $binary_remote_addr zone=conn_limit_per_ip:10m;
limit_conn_zone $server_name zone=conn_limit_server:10m;
limit_req_status 429;
limit_conn_status 429;
