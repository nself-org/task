# GENERATED BY nself build - DO NOT HAND EDIT
# Run 'nself build' to regenerate this file.
name: backend
networks:
    backend_network:
        driver: bridge
volumes:
    postgres_data: {}
    redis_data: {}
services:
    postgres:
        image: pgvector/pgvector:pg16
        container_name: backend_postgres
        restart: unless-stopped
        shm_size: 256mb
        user: 999:999
        networks:
            - backend_network
        environment:
            PGDATA: /var/lib/postgresql/data/pgdata
            POSTGRES_DB: nself
            POSTGRES_HOST_AUTH_METHOD: scram-sha-256
            POSTGRES_PASSWORD: 6gsF6v39YDl9uWTuOrJaIB7PjGLPRta4
            POSTGRES_USER: postgres
        volumes:
            - postgres_data:/var/lib/postgresql/data
            - ./postgres/init:/docker-entrypoint-initdb.d:ro
        ports:
            - 127.0.0.1:5432:5432
        healthcheck:
            test:
                - CMD-SHELL
                - pg_isready -U postgres
            interval: 10s
            timeout: 5s
            retries: 5
        deploy:
            resources:
                limits:
                    memory: 2g
                    cpus: "2.0"
                    pids: 100
                reservations:
                    memory: 256m
        logging:
            driver: json-file
            options:
                max-file: "3"
                max-size: 10m
        security_opt:
            - no-new-privileges:true
        cap_drop:
            - ALL
        cap_add:
            - CHOWN
            - SETUID
            - SETGID
            - IPC_LOCK
        stop_grace_period: 30s
        read_only: true
        tmpfs:
            - /tmp
            - /run/postgresql
    hasura:
        image: hasura/graphql-engine:v2.44.0
        container_name: backend_hasura
        restart: unless-stopped
        user: 1001:1001
        networks:
            - backend_network
        depends_on:
            postgres:
                condition: service_healthy
        environment:
            HASURA_GRAPHQL_ADMIN_SECRET: 3886f530c54a8272b4bd21e0eb1f6ada2403931b79beee26c2f1774ad4ae3eac
            HASURA_GRAPHQL_CORS_DOMAIN: http://localhost:*
            HASURA_GRAPHQL_DATABASE_URL: postgresql://postgres:6gsF6v39YDl9uWTuOrJaIB7PjGLPRta4@postgres:5432/nself
            HASURA_GRAPHQL_DEV_MODE: ${HASURA_GRAPHQL_DEV_MODE:-false}
            HASURA_GRAPHQL_ENABLE_CONSOLE: ${HASURA_GRAPHQL_ENABLE_CONSOLE:-false}
            HASURA_GRAPHQL_ENABLE_TELEMETRY: "false"
            HASURA_GRAPHQL_JWT_SECRET: '{"key":"p1YJKERu4v7XFViQEDH1kb2mskYr-Xc3OKVpjlqPfLRt","type":"HS256"}'
            HASURA_GRAPHQL_LOG_LEVEL: warn
            HASURA_GRAPHQL_UNAUTHORIZED_ROLE: public
        ports:
            - 127.0.0.1:8080:8080
        healthcheck:
            test:
                - CMD
                - curl
                - -f
                - http://localhost:8080/healthz
            interval: 10s
            timeout: 5s
            retries: 5
            start_period: 15s
        deploy:
            resources:
                limits:
                    memory: 1g
                    cpus: "1.0"
                    pids: 100
                reservations:
                    memory: 256m
        logging:
            driver: json-file
            options:
                max-file: "3"
                max-size: 10m
        security_opt:
            - no-new-privileges:true
        cap_drop:
            - ALL
        stop_grace_period: 30s
        read_only: true
        tmpfs:
            - /tmp
            - /run
    auth:
        image: nhost/hasura-auth:0.36.0
        container_name: backend_auth
        restart: unless-stopped
        networks:
            - backend_network
        depends_on:
            hasura:
                condition: service_healthy
            postgres:
                condition: service_healthy
        environment:
            AUTH_ACCESS_TOKEN_EXPIRES_IN: "900"
            AUTH_ALLOWED_REDIRECT_URLS: https://localhost,https://*.localhost,http://localhost:3000
            AUTH_CLIENT_URL: http://localhost:3000
            AUTH_DATABASE_URL: postgresql://postgres:6gsF6v39YDl9uWTuOrJaIB7PjGLPRta4@postgres:5432/nself
            AUTH_DB_HOST: postgres
            AUTH_DB_NAME: nself
            AUTH_DB_PASSWORD: 6gsF6v39YDl9uWTuOrJaIB7PjGLPRta4
            AUTH_DB_PORT: "5432"
            AUTH_DB_URL: postgresql://postgres:6gsF6v39YDl9uWTuOrJaIB7PjGLPRta4@postgres:5432/nself
            AUTH_DB_USER: postgres
            AUTH_EMAIL_SIGNIN_EMAIL_VERIFIED_REQUIRED: "false"
            AUTH_HOST: 0.0.0.0
            AUTH_JWT_SECRET: sKICdzHUr8968LEQpcCarmIPwH8o-14KnD0oAeW0vfEZ
            AUTH_JWT_TYPE: HS256
            AUTH_LOG_LEVEL: info
            AUTH_PORT: "4000"
            AUTH_REFRESH_TOKEN_EXPIRES_IN: "43200"
            AUTH_SERVER_URL: http://localhost:4000
            AUTH_SMTP_HOST: mailpit
            AUTH_SMTP_PASS: ""
            AUTH_SMTP_PORT: "1025"
            AUTH_SMTP_SECURE: "false"
            AUTH_SMTP_SENDER: noreply@localhost
            AUTH_SMTP_USER: ""
            AUTH_WEBAUTHN_ENABLED: "false"
            DATABASE_URL: postgresql://postgres:6gsF6v39YDl9uWTuOrJaIB7PjGLPRta4@postgres:5432/nself
            HASURA_GRAPHQL_ADMIN_SECRET: 3886f530c54a8272b4bd21e0eb1f6ada2403931b79beee26c2f1774ad4ae3eac
            HASURA_GRAPHQL_DATABASE_URL: postgresql://postgres:6gsF6v39YDl9uWTuOrJaIB7PjGLPRta4@postgres:5432/nself
            HASURA_GRAPHQL_GRAPHQL_URL: http://hasura:8080/v1/graphql
            HASURA_GRAPHQL_JWT_SECRET: '{"key":"p1YJKERu4v7XFViQEDH1kb2mskYr-Xc3OKVpjlqPfLRt","type":"HS256"}'
            POSTGRES_HOST: postgres
            POSTGRES_PORT: "5432"
        ports:
            - 127.0.0.1:4000:4000
        healthcheck:
            test:
                - CMD
                - wget
                - -qO-
                - http://localhost:4000/healthz
            interval: 30s
            timeout: 10s
            retries: 3
            start_period: 30s
        deploy:
            resources:
                limits:
                    memory: 256m
                    cpus: "0.25"
                    pids: 100
                reservations:
                    memory: 256m
        logging:
            driver: json-file
            options:
                max-file: "3"
                max-size: 10m
        security_opt:
            - no-new-privileges:true
        cap_drop:
            - ALL
        stop_grace_period: 30s
        read_only: true
        tmpfs:
            - /tmp
            - /run
    redis:
        image: redis:7.2-alpine
        container_name: backend_redis
        restart: unless-stopped
        user: 999:999
        networks:
            - backend_network
        volumes:
            - redis_data:/data
        ports:
            - 127.0.0.1:6379:6379
        command: redis-server --appendonly yes --protected-mode yes --requirepass ${REDIS_PASSWORD}
        healthcheck:
            test:
                - CMD
                - redis-cli
                - -a
                - ${REDIS_PASSWORD}
                - ping
            interval: 10s
            timeout: 5s
            retries: 5
        deploy:
            resources:
                limits:
                    memory: 512M
                    cpus: "0.5"
                    pids: 100
                reservations:
                    memory: 256m
        logging:
            driver: json-file
            options:
                max-file: "3"
                max-size: 10m
        security_opt:
            - no-new-privileges:true
        cap_drop:
            - ALL
        stop_grace_period: 30s
        read_only: true
        tmpfs:
            - /tmp
    nginx:
        image: nginx:1.25-alpine
        container_name: backend_nginx
        restart: unless-stopped
        user: 101:101
        networks:
            - backend_network
        depends_on:
            auth:
                condition: service_healthy
            hasura:
                condition: service_healthy
        environment:
            BASE_DOMAIN: localhost
            ENV: dev
            PROJECT_NAME: backend
        volumes:
            - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
            - ./nginx/conf.d:/etc/nginx/conf.d:ro
            - ./nginx/conf.d-dev:/etc/nginx/conf.d-dev:ro
            - ./nginx/sites:/etc/nginx/sites:ro
            - ./nginx/includes:/etc/nginx/includes:ro
            - ./ssl:/etc/nginx/ssl:ro
        ports:
            - 127.0.0.1:80:80
            - 127.0.0.1:443:443
        healthcheck:
            test:
                - CMD-SHELL
                - wget --no-check-certificate --no-verbose --tries=1 -O /dev/null https://127.0.0.1/health 2>/dev/null || exit 1
            interval: 30s
            timeout: 5s
            retries: 3
            start_period: 10s
        deploy:
            resources:
                limits:
                    memory: 256m
                    cpus: "0.5"
                    pids: 100
                reservations:
                    memory: 256m
        logging:
            driver: json-file
            options:
                max-file: "3"
                max-size: 10m
        security_opt:
            - no-new-privileges:true
        cap_drop:
            - ALL
        cap_add:
            - NET_BIND_SERVICE
        stop_grace_period: 30s
        read_only: true
        tmpfs:
            - /var/cache/nginx:uid=101,gid=101
            - /var/run:uid=101,gid=101
