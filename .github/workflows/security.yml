name: Security Scan

# Disabled: CodeQL JS/TS scanning removed — repo is now Flutter (Dart).
# CodeQL does not support Dart. Re-enable if native platform code is added.
on:
  workflow_dispatch:

jobs:
  placeholder:
    name: Security Scan (disabled)
    runs-on: ubuntu-latest
    steps:
      - run: echo "CodeQL not applicable to Dart/Flutter. Re-enable when native code is added."
